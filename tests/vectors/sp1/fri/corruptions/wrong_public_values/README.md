# wrong_public_values — negative fixture

Generated by `tests/vectors/sp1/fri/corruptions/gen.go` from
`../minimal-guest/`. Do not hand-edit — re-run the generator.

## Mutation

Flip one byte of `public_values.hex` (the low byte of the third u32).

| | |
|---|---|
| Mutated file | `public_values.hex` |
| Decoded field | `public_values[2] (`x`, the claimed final Fibonacci value)` |
| Byte offset | 8 (0x8) |
| Original byte | 0x15 |
| Mutated byte | 0x14 |
| Original value | 21 |
| Mutated value | 20 |
| Unchanged copy | `proof.postcard` |

## Detection point

| | |
|---|---|
| Documented (docs/sp1-fri-verifier.md §6) | Transcript divergence |
| Actually observed | Transcript divergence, surfacing at the commit-phase PoW witness |

Rejection message:

```text
verify: FRI: fri: commit-phase round 0 invalid PoW witness
```

MATCHES the documented detection point. `publicValues` is absorbed by the
challenger in verify.go step 2, so every downstream challenge (alpha, zeta,
every beta, every query index) shifts. The first check that consumes a
post-divergence challenge is the commit-phase grinding witness.

## Asserted by

`compilers/go/codegen/sp1_fri_negative_test.go`
(`TestSp1FriVerifier_RejectsCorruptionFixtures`), which re-derives the byte
offset from the real decoder and asserts the exact rejection message above.
