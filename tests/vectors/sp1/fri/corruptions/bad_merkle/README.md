# bad_merkle — negative fixture

Generated by `tests/vectors/sp1/fri/corruptions/gen.go` from
`../minimal-guest/`. Do not hand-edit — re-run the generator.

## Mutation

Flip one byte in the first sibling digest of the first input-MMCS Merkle path.

| | |
|---|---|
| Mutated file | `proof.postcard` |
| Decoded field | `opening_proof.query_proofs[0].input_proof[0].opening_proof[0][0]` |
| Byte offset | 311 (0x137) |
| Original byte | 0xf6 |
| Mutated byte | 0xf7 |
| Original value | 797723638 |
| Mutated value | 797723639 |
| Unchanged copy | `public_values.hex` |

field varint spans bytes [311, 316) — the mutation keeps the varint length identical so the file size and every later offset are unchanged.

## Detection point

| | |
|---|---|
| Documented (docs/sp1-fri-verifier.md §6) | Merkle root recompute |
| Actually observed | Merkle root recompute |

Rejection message:

```text
verify: FRI: fri: query 0 open_input: input MMCS verify (batch 0): verify_batch: cap mismatch at idx 0
```

MATCHES the documented detection point. The mutated limb is a sibling
hash on the authentication path, so `VerifyBatch` recomputes a root that
does not equal the committed MerkleCap.

## Asserted by

`compilers/go/codegen/sp1_fri_negative_test.go`
(`TestSp1FriVerifier_RejectsCorruptionFixtures`), which re-derives the byte
offset from the real decoder and asserts the exact rejection message above.
