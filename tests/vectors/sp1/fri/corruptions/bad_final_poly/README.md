# bad_final_poly — negative fixture

Generated by `tests/vectors/sp1/fri/corruptions/gen.go` from
`../minimal-guest/`. Do not hand-edit — re-run the generator.

## Mutation

Change the first limb of the first final-poly Ext4 coefficient.

| | |
|---|---|
| Mutated file | `proof.postcard` |
| Decoded field | `opening_proof.final_poly[0].c0` |
| Byte offset | 1508 (0x5e4) |
| Original byte | 0xa7 |
| Mutated byte | 0xa6 |
| Original value | 1319737639 |
| Mutated value | 1319737638 |
| Unchanged copy | `public_values.hex` |

field varint spans bytes [1508, 1513) — the mutation keeps the varint length identical so the file size and every later offset are unchanged.

## Detection point

| | |
|---|---|
| Documented (docs/sp1-fri-verifier.md §6) | Final-poly equality |
| Actually observed | input-MMCS Merkle root recompute, via transcript divergence |

Rejection message:

```text
verify: FRI: fri: query 0 open_input: input MMCS verify (batch 0): verify_batch: cap mismatch at idx 0
```

DIVERGES from the documented "final-poly equality". `final_poly` is observed
into the Fiat-Shamir transcript (fri.go step 4) BEFORE the query indices are
sampled, so mutating it re-randomises every query index; the Merkle proof
then authenticates the wrong leaf and MMCS fails first. The
`fri: query N final_poly mismatch` branch is unreachable by byte-level
corruption for exactly this reason.

## Asserted by

`compilers/go/codegen/sp1_fri_negative_test.go`
(`TestSp1FriVerifier_RejectsCorruptionFixtures`), which re-derives the byte
offset from the real decoder and asserts the exact rejection message above.
