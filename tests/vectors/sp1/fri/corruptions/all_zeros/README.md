# all_zeros — negative fixture

Generated by `tests/vectors/sp1/fri/corruptions/gen.go` from
`../minimal-guest/`. Do not hand-edit — re-run the generator.

## Mutation

204800 bytes of 0x00 in place of `proof.postcard`.

| | |
|---|---|
| Mutated file | `proof.postcard` |
| Size | 1589 bytes -> 204800 bytes |
| Unchanged copy | `public_values.hex` |

## Detection point

| | |
|---|---|
| Documented (docs/sp1-fri-verifier.md §6) | bincode length / hash |
| Actually observed | postcard top-level length check |

Rejection message:

```text
postcard: trailing bytes after top-level decode: 204785 bytes left
```

MATCHES the documented detection point. An all-zero buffer decodes as a
structurally minimal proof (every Vec length is 0) and consumes 15 of the
204800 bytes; the strict trailing-bytes check in `DecodeProof` rejects it.

## Asserted by

`compilers/go/codegen/sp1_fri_negative_test.go`
(`TestSp1FriVerifier_RejectsCorruptionFixtures`), which re-derives the byte
offset from the real decoder and asserts the exact rejection message above.
