# truncated — negative fixture

Generated by `tests/vectors/sp1/fri/corruptions/gen.go` from
`../minimal-guest/`. Do not hand-edit — re-run the generator.

## Mutation

Strip the last 100 bytes of `proof.postcard`.

| | |
|---|---|
| Mutated file | `proof.postcard` |
| Size | 1589 bytes -> 1489 bytes |
| Unchanged copy | `public_values.hex` |

## Detection point

| | |
|---|---|
| Documented (docs/sp1-fri-verifier.md §6) | Push-and-hash binding |
| Actually observed | Two independent points, both asserted |

Rejection message:

```text
postcard decode: `opening_proof: query_proofs: query[1]: commit_phase_openings:
step[0]: opening_proof: [3]: digest[4]: postcard: unexpected EOF at offset 1489`
push-and-hash: `OP_EQUALVERIFY` failure in the Step 1 SHA-256 binding
```

PARTIALLY MATCHES. Off-chain the truncated blob never reaches the verifier —
the postcard decoder hits EOF first. On-chain there is no decoder, so the
documented push-and-hash binding IS the detection point and is asserted
directly against `EmitProofBlobBindingHash` by the codegen negative test.

## Asserted by

`compilers/go/codegen/sp1_fri_negative_test.go`
(`TestSp1FriVerifier_RejectsCorruptionFixtures`), which re-derives the byte
offset from the real decoder and asserts the exact rejection message above.
