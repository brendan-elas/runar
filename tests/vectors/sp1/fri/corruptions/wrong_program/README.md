# wrong_program — negative fixture

Generated by `tests/vectors/sp1/fri/corruptions/gen.go` from
`../minimal-guest/`. Do not hand-edit — re-run the generator.

## Mutation

The unmodified `minimal-guest` proof paired with the `evm-guest` program's
public values. No byte of the proof is touched — the *binding* is wrong.

| | |
|---|---|
| Mutated file | `public_values.hex (replaced wholesale)` |
| Unchanged copy | `proof.postcard (byte-identical to minimal-guest)` |

## Detection point

| | |
|---|---|
| Documented (docs/sp1-fri-verifier.md §6) | Transcript divergence |
| Actually observed | Transcript divergence, surfacing at the query-phase PoW witness |

Rejection message:

```text
verify: FRI: fri: invalid query PoW witness
```

MATCHES the documented detection point, but DEVIATES from the documented
*mutation*: the original matrix says "minimal-guest proof + EVM-guest VK
hash". No VK hash exists anywhere in this fixture tree (see ../bad_vk/),
so the program identity is expressed through the public values instead —
the only program-identifying input the reference verifier actually consumes.

## Asserted by

`compilers/go/codegen/sp1_fri_negative_test.go`
(`TestSp1FriVerifier_RejectsCorruptionFixtures`), which re-derives the byte
offset from the real decoder and asserts the exact rejection message above.
